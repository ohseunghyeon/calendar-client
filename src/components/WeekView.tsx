import React from 'react';

const WeekView: React.FC<{
  events: {
    id: string;
    title: string;
    start: number;
    end: number;
  }[];
}> = () => {
  return <div></div>;
};

export default WeekView;
